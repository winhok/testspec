---
name: testspec-review
description: TestSpec 用例评审（流程第 5 步）- 对生成的测试用例做 12 维度交叉验证（R1-R6 规则检查 + H1-H6 启发式检查），产出 review-report.md 评审报告。当用户要「评审用例」「检查用例质量」「审查测试用例」或执行 testspec-review / testspec review 时使用。也适用于用户说「用例写完了帮我看看」「检查一下覆盖度」「用例有没有问题」的场景。支持默认模式和 --deep 深度模式。
---

# testspec-review：用例评审

## 职责

以独立评审视角，对已生成的测试用例进行交叉验证，检查覆盖度、命名规范、优先级分布、字段完整性、可执行性、可追溯性、冗余检测、预期结果质量、意图一致性、前置条件充分性、风险与边界覆盖、可维护性等 12 个维度，输出结构化的评审报告（review-report.md），帮助团队识别用例质量问题并提供整改建议。

## 当前变更目录

参见 `../testspec-shared/common.md` 中的「当前变更目录定位规则」。

---

## 输入策略

### 必读文件

- **testcases.json**：生成的测试用例 JSON 文件（位于变更目录根目录或 artifacts/ 下）
- **specs/testpoints.md**：测试点文档（用于覆盖度检查和意图一致性验证）

### 可选文件（deep 模式）

- **requirements-analysis.md**：需求分析文档（deep 模式下用于增强意图一致性和风险覆盖检查）

### 输入健康检查（Step 2.5）

在执行评审前，必须进行以下健康检查：

1. **testcases.json 存在性检查**：
   - 文件存在且非空
   - JSON 格式有效（可解析）
   - 包含 `testcases` 数组且数组非空

2. **testpoints.md 存在性检查**：
   - 文件存在且非空
   - 包含测试点标识（TP_ID）

3. **健康检查失败处理**：
   - 若任一必读文件缺失或无效，输出错误信息并终止评审
   - 提示用户先执行 testspec-generate 或 testspec-points

### 模式判定（Strict/Legacy）

根据 testcases.json 的 schema 判定评审模式：

- **Strict 模式**：
  - testcases.json 包含 `schema_version: 2`
  - 所有用例包含 `tp_refs` 字段且非空
  - 可进行精确的可追溯性检查和意图一致性验证

- **Legacy 模式**：
  - testcases.json 不包含 `schema_version` 或 `schema_version < 2`
  - 用例缺少 `tp_refs` 字段
  - 可追溯性检查降级为警告（⚠️ S2）
  - 意图一致性检查 fallback 到标题匹配，置信度下调

---

## 评审维度

### 规则检查（R1-R6）

#### R1: 测试点覆盖度

**检查目标**：验证测试用例是否充分覆盖测试点文档中的所有测试点。

**检查方法**：
1. 从 testpoints.md 提取所有 TP_ID
2. Strict 模式：统计 testcases.json 中所有用例的 `tp_refs` 字段，计算覆盖率
3. Legacy 模式：通过标题匹配推断覆盖情况（置信度较低）
4. 计算覆盖率 = 已覆盖 TP 数 / 总 TP 数 × 100%

**判定标准**：
- ✅ 通过：覆盖率 ≥ 95%
- ❌ 失败（S2）：覆盖率 < 95%

**输出**：
- 覆盖率百分比
- 未覆盖的 TP_ID 列表
- 每个未覆盖 TP 的影响说明和整改建议

---

#### R2: 命名契约

**检查目标**：验证用例标题是否符合三段式命名规范。

**检查方法**：
1. 遍历所有用例的 `title` 字段
2. 检查格式是否为 `{模块}_{功能点}_{场景}`（使用下划线分隔）
3. 验证 `{模块}` 是否与 `feature` 字段一致
4. 验证 `{模块}` 和 `{功能点}` 是否与 testpoints.md 中的标题层级严格一致

**判定标准**：
- ✅ 通过：所有用例标题符合命名契约
- ❌ 失败（S2）：存在违规用例

**输出**：
- 违规用例数
- 每个违规用例的 ID、当前标题、违规原因、建议标题

---

#### R3: 优先级分布

**检查目标**：验证用例优先级分布是否合理。

**检查方法**：
1. 统计冒烟用例、P1、P2、P3 用例数量
2. 计算各级别占比
3. 检查冒烟用例是否全部为 P1

**判定标准**：
- ✅ 通过：
  - 冒烟用例占比 30% ± 10%
  - P1 用例占比 50% ± 10%
  - P2 用例占比 30% ± 10%
  - P3 用例占比 20% ± 10%
  - 所有冒烟用例均为 P1
- ⚠️ 警告（S3）：占比超出合理范围

**输出**：
- 各级别用例数和占比
- 分布异常的级别及建议调整方向

---

#### R4: 字段完整性

**检查目标**：验证用例必填字段是否完整。

**检查方法**：
1. 遍历所有用例
2. 检查必填字段：`id`、`title`、`steps`、`expected_result`、`priority`
3. 检查字段值是否非空（去除空白字符后长度 > 0）

**判定标准**：
- ✅ 通过：所有用例的必填字段均非空
- ❌ 失败（S1）：存在缺失或空字段

**输出**：
- 缺失字段的用例数
- 每个问题用例的 ID、缺失字段列表、整改建议

---

#### R5: 可执行性最小条件

**检查目标**：验证用例是否满足可执行的最小条件。

**检查方法**：
1. 检查 `steps` 和 `expected_result` 长度是否 ≥ 10 字符
2. 检查 `steps` 是否包含动作动词（点击、输入、选择、等待、查看、校验、打开、提交、确认、删除、修改、搜索、上传、下载）

**判定标准**：
- ✅ 通过：所有用例满足最小条件
- ❌ 失败（S1）：存在不满足条件的用例

**输出**：
- 不满足条件的用例数
- 每个问题用例的 ID、不满足的条件、整改建议

---

#### R6: 可追溯性

**检查目标**：验证用例与测试点的追溯关系是否完整。

**检查方法**：
- **Strict 模式**：
  1. 检查所有用例是否包含 `tp_refs` 字段
  2. 检查 `tp_refs` 是否非空且包含有效的 TP_ID
  3. 计算 tp_refs 映射率 = 有 tp_refs 的用例数 / 总用例数 × 100%
- **Legacy 模式**：
  1. 检测到缺少 `tp_refs` 字段
  2. 输出警告（⚠️ S2），提示重新生成用例以支持精确追溯

**判定标准**：
- **Strict 模式**：
  - ✅ 通过：tp_refs 映射率 = 100%
  - ❌ 失败（S1）：tp_refs 映射率 < 100%
- **Legacy 模式**：
  - ⚠️ 警告（S2）：无 tp_refs 字段，建议重新生成

**输出**：
- 追溯完整性百分比
- 缺少 tp_refs 的用例列表
- Legacy 模式下的升级建议

---

### 启发式检查（H1-H6）

#### H1: 冗余检测

**检查目标**：识别可能冗余的测试用例。

**检查方法**（两阶段）：
1. **候选生成阶段**：
   - 计算用例间的相似度（基于标题、步骤、预期结果）
   - 标题相似度 > 80% 或步骤相似度 > 70% 的用例对标记为候选
2. **AI 判断阶段**：
   - 对候选用例对，使用 LLM 判断是否真正冗余
   - 考虑测试场景、数据、验证点的差异

**判定标准**：
- ✅ 通过：无疑似冗余用例对
- ⚠️ 警告（S3）：存在疑似冗余用例对

**输出**：
- 疑似冗余用例对数
- 每对用例的 ID、相似度、冗余原因、合并建议

---

#### H2: 预期结果质量

**检查目标**：检测预期结果中的模糊表述。

**检查方法**：
1. 遍历所有用例的 `expected_result` 字段
2. 检测模糊词汇：正常、成功、正确、显示、提示（无具体内容）
3. 检查是否缺少具体的验证标准（数值、状态、文案）

**判定标准**：
- ✅ 通过：所有预期结果具体、可验证
- ⚠️ 警告（S3）：存在模糊表述

**输出**：
- 模糊表述用例数
- 每个问题用例的 ID、模糊表述内容、改进建议

---

#### H3: 意图一致性

**检查目标**：验证用例是否准确反映测试点的验证意图。

**检查方法**：
- **Strict 模式**：
  1. 按 TP_ID 分组用例（通过 `tp_refs` 字段）
  2. 对每个 TP，读取其验证要点
  3. 逐条评估用例与 TP 的一致性，三段评分：
     - **Action Match**（操作匹配）：用例步骤是否覆盖 TP 要求的操作
     - **Oracle Match**（预期匹配）：用例预期结果是否覆盖 TP 的验证点
     - **Scope Match**（范围匹配）：用例是否引入了 TP 之外的验证内容
  4. 综合评分：三项均达标 → 一致，否则 → 偏移
- **Legacy 模式**：
  1. Fallback 到标题匹配（通过标题前缀推断 TP）
  2. 置信度下调，评估结果标注为"低置信度"

**预算阀门**：
- 默认模式：对高风险 TP（P1、冒烟相关）全解释
- Deep 模式：对所有 TP 全解释

**判定标准**：
- ✅ 通过：所有用例与 TP 意图一致
- ❌ 失败（S1）：存在意图偏移的用例

**输出**：
- 意图一致性评估表（TP_ID、用例数、Action Match、Oracle Match、Scope Match、结论、证据/说明）
- 意图偏移的用例列表及整改建议

---

#### H4: 前置条件充分性

**检查目标**：验证用例前置条件是否充分、可执行。

**检查方法**（抽样策略）：
1. 每个模块抽样：
   - P1 用例：3 条
   - 冒烟用例：3 条
   - 随机用例：3 条
2. 对抽样用例，检查前置条件是否：
   - 明确具体（无模糊表述）
   - 可执行（有明确的准备步骤）
   - 完整（覆盖所有必要的前置状态）

**判定标准**：
- ✅ 通过：抽样用例的前置条件均充分
- ⚠️ 警告（S2）：存在前置条件不充分的用例

**输出**：
- 抽样数和不充分数
- 每个问题用例的 ID、前置条件问题、改进建议

---

#### H5: 风险与边界覆盖

**检查目标**：验证用例是否覆盖多种测试类型（正向、负向、边界、异常、冒烟）。

**检查方法**：
1. 统计各类型用例数量
2. 检查覆盖的类型数

**判定标准**：
- ✅ 通过（默认模式）：覆盖 ≥ 2 种类型
- ⚠️ 警告（S2，默认模式）：覆盖 < 2 种类型
- ❌ 失败（S2，deep 模式）：覆盖 < 3 种类型

**输出**：
- 覆盖的类型列表和各类型用例数
- 缺失的类型及补充建议

---

#### H6: 可维护性建议

**检查目标**：提供用例可维护性优化建议。

**检查方法**：
1. 基于 H1 冗余检测结果，提供合并建议
2. 检查用例粒度是否适中（步骤数 3-10 为宜）
3. 检查用例是否过于复杂（步骤数 > 15）

**判定标准**：
- ✅ 通过：无明显可维护性问题
- ⚠️ 警告（S3）：存在可优化项

**输出**：
- 建议数
- 每条建议的类型（冗余合并、粒度调整、复杂度简化）、涉及用例、具体建议

---

## 严重级别体系

评审问题按严重程度分为三级：

- **S1 阻断级**：必须修复，否则用例不可执行或严重偏离测试意图
  - 示例：缺少必填字段、不满足可执行性最小条件、意图偏移、tp_refs 映射不完整（Strict 模式）
- **S2 重要级**：应当修复，影响用例质量和可维护性
  - 示例：命名违规、覆盖率不足、前置条件不充分、无 tp_refs（Legacy 模式）
- **S3 建议级**：可选优化，提升用例质量和可维护性
  - 示例：冗余用例、预期结果模糊、优先级分布不均、可维护性改进

---

## 执行步骤

1. **定位当前变更目录**（按 common.md 规则）
2. **读取必读文件**：
   - testcases.json（变更目录根目录或 artifacts/ 下）
   - specs/testpoints.md
3. **输入健康检查 + 模式判定**（Step 2.5）：
   - 检查 testcases.json 存在性、格式有效性、非空性
   - 检查 testpoints.md 存在性、非空性
   - 判定评审模式（Strict/Legacy）
   - 若健康检查失败，输出错误信息并终止
4. **执行规则检查**（R1-R6）：
   - 按顺序执行 6 项规则检查
   - 记录每项检查的结果、指标、问题列表
5. **执行启发式检查**（H1-H6）：
   - 按顺序执行 6 项启发式检查
   - 记录每项检查的结果、指标、问题列表
6. **生成评审报告**：
   - 使用 `review-report-template.md` 作为模板
   - 填充所有检查结果、问题列表、整改建议
   - 计算总体置信度（Strict 模式 + 所有检查通过 → 高置信度；Legacy 模式 → 中置信度；输入不完整 → 低置信度）
   - 输出 review-report.md 到变更目录根目录
7. **输出总结**：
   - 报告路径
   - 总评结论（通过/有问题）
   - S1 阻断级问题数
   - 评审模式（Strict/Legacy）
   - 置信度（高/中/低）

---

## 触发方式

### 默认模式

```bash
testspec-review
```

- 读取 testcases.json + testpoints.md
- 执行所有 12 项检查
- H3 意图一致性检查仅对高风险 TP 全解释
- H5 风险覆盖检查要求 ≥ 2 种类型

### Deep 模式

```bash
testspec-review --deep
```

- 额外读取 requirements-analysis.md
- H3 意图一致性检查对所有 TP 全解释
- H5 风险覆盖检查要求 ≥ 3 种类型
- 提供更详细的问题分析和整改建议

---

## 产出

**review-report.md**：结构化评审报告，位于变更目录根目录。

报告结构参见 `review-report-template.md`，包含：
- 总评（结论、置信度、结论总览矩阵）
- 规则检查详情（R1-R6）
- 启发式检查详情（H1-H6）
- 整改建议清单（按 S1/S2/S3 分级）

---

## 总体置信度计算

评审报告的置信度取决于输入完整性和评审模式：

- **高置信度**：
  - Strict 模式（有 tp_refs）
  - 所有必读文件完整
  - 所有检查项均可执行

- **中置信度**：
  - Legacy 模式（无 tp_refs）
  - H3 意图一致性和 R6 可追溯性检查结论可信度下降

- **低置信度**：
  - 输入文件不完整（缺少 testpoints.md 或 testcases.json 格式异常）
  - 部分检查项无法执行

---

## 依赖

- Python 3.x（用于 JSON 解析和文本分析）
- testcases.json（schema v2 推荐，v1 可降级支持）
- specs/testpoints.md（必须）
- requirements-analysis.md（deep 模式可选）

---

## 示例输出

```
✅ 评审完成

📄 报告路径: /path/to/testspec/changes/login-feature/review-report.md

📊 总评: 发现 2 个 S1 阻断级问题，5 个 S2 重要级问题，3 个 S3 建议级问题

🔍 评审模式: Strict
📈 置信度: 高

⚠️ S1 阻断级问题:
- [R5] 3 个用例不满足可执行性最小条件（缺少动作动词）
- [H3] 2 个用例意图偏移（验证点与 TP 不一致）

建议优先修复 S1 问题后再执行测试。
```
